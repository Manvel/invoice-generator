{
  "name": "invoice-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "setup": "mkdir -p dist && cp index.html styles.css dist/",
    "dev": "npm run setup && esbuild src/app.js --bundle --outfile=dist/bundle.js --watch --servedir=dist",
    "build": "npm run setup && esbuild src/app.js --bundle --outfile=dist/bundle.js",
    "test": "node test/numberToWords.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  },
  "dependencies": {
    "lit-html": "^3.1.0"
  }
}
