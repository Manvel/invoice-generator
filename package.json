{
  "name": "invoice-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/app.js --bundle --outfile=dist/bundle.js --watch --servedir=.",
    "build": "esbuild src/app.js --bundle --outfile=dist/bundle.js",
    "test": "node test/numberToWords.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  },
  "dependencies": {
    "lit-html": "^3.1.0"
  }
}
