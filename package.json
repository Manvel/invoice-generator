{
  "name": "invoice-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "setup": "mkdir -p dist && cp src/index.html src/data.js styles.css dist/",
    "dev": "node build.js --watch",
    "build": "node build.js",
    "test": "node test/numberToWords.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  },
  "dependencies": {
    "lit-html": "^3.1.0"
  }
}
