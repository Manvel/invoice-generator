{
  "name": "invoice-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "http-server . -o",
    "build": "esbuild src/app.js --bundle --outfile=dist/bundle.js",
    "watch": "esbuild src/app.js --bundle --outfile=dist/bundle.js --watch"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "esbuild": "^0.20.1"
  },
  "dependencies": {
    "lit-html": "^3.1.0"
  }
}
